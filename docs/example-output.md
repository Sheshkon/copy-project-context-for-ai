# Example Output

This is a realistic example of output generated by **Copy Project Context for AI** for a small sample project.

~~~
Project Structure

src/
├── main/
│   ├── java/
│   │   └── com/
│   │       └── example/
│   │           └── App.java
│   └── resources/
│       └── application.yml
└── test/
    └── java/
        └── com/
            └── example/
                └── AppTest.java

==================================================
FILE: src/main/java/com/example/App.java
==================================================

```java
package com.example;

public class App {
    public static void main(String[] args) {
        System.out.println("Hello, world!");
    }
}
```

==================================================
FILE: src/main/resources/application.yml
==================================================

```yaml
server:
  port: 8080

spring:
  application:
    name: demo
```

==================================================
FILE: src/test/java/com/example/AppTest.java
==================================================

```java
package com.example;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

class AppTest {
    @Test
    void contextLoads() {
        assertTrue(true);
    }
}
```
~~~

Notes:

- Paths are always relative to the project root.
- File content is preserved exactly as stored on disk.
- Language identifiers are inferred from file extensions.
- The tree includes only files that were actually exported.
